import styled from "styled-components";
import { SPACING } from "@/shared/lib/responsive";

export const Container = styled.div({
  padding: SPACING.lg,
  minHeight: "100vh",
  backgroundColor: "#f5f5f5",
  maxWidth: 1400,
  margin: "0 auto",
});

export const SeoCard = styled.div({
  maxWidth: 1400,
  margin: "0 auto",
});
